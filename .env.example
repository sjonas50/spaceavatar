# Commander Sky — environment template. Copy to .env (never commit .env).
# All keys are server-side only; nothing here ever reaches the browser.

# LiveKit Cloud (Ship plan or above — Build plan has 10-20s cold starts)
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Model providers
ANTHROPIC_API_KEY=
DEEPGRAM_API_KEY=
CARTESIA_API_KEY=
LEMONSLICE_API_KEY=

# Avatar rendering path: lemonslice | frontend | none  (Phase 2 bake-off decides)
AVATAR_MODE=lemonslice
# LemonSlice character: either a saved agent ID or a single character image URL.
# Tip: use a waist-up image with hands visible (368x560 portrait) — the model
# only animates what's in frame, so a head-shot gets no hand motion.
LEMONSLICE_AGENT_ID=
LEMONSLICE_IMAGE_URL=

# Avatar motion/demeanor prompts (energy and expressiveness, not exact gestures).
# Defaults in config.py; override here to experiment.
#AVATAR_PROMPT=
#AVATAR_IDLE_PROMPT=

# Model overrides (defaults in agent/src/commander_sky/config.py)
#LLM_MODEL=claude-sonnet-4-6
#STT_MODEL=flux-general-en
#TTS_MODEL=sonic-3.5
#TTS_VOICE=

# Session policy
MAX_SESSION_MINUTES=15
MAX_SESSION_COST_USD=5.0

# Web abuse controls (see docs/deploy.md). Upstash is REQUIRED for public
# deploys — without it rate limits are per-instance memory only.
#UPSTASH_REDIS_REST_URL=
#UPSTASH_REDIS_REST_TOKEN=
#SESSIONS_PER_IP_PER_HOUR=6
#MAX_SESSIONS_PER_DAY=50
#ACCESS_CODE=

# Kid-tuned turn taking
ENDPOINTING_DELAY_S=0.9
BARGE_IN_MIN_WORDS=3
